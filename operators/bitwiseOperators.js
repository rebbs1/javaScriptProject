/*let A = 1;
let B = 2;
console.log(A & B)                  // AND Bitwise operator wch sets each bit to 1 only if both bits are 1
let A = 1;
let B = 1;
console.log(A & B) */                  // AND Bitwise operator wch sets each bit to 1 only if both bits are 1

let A = 1;
let B = 2;
console.log(A | B)                  // OR Bitwise operator wch sets each bit to 1 if any of the bits is 1

let X = 3;
let Y = 5;
console.log(X ^ Y)                  // OR Bitwise operator wch sets each bit to 1 if any of the bits is 1

console.log(~5)                    //This inverts all bits meaning 1 becomes 0 and 0 becomes 1

console.log(5 << 1)                 // This shifts left and converts empty spaces to zero

console.log(5 >> 1)                 // This shifts right and converts empty spaces to zero